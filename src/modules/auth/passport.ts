// Passport.js configuration for Firebase Authentication
// Este arquivo pode ser usado para estratégias de autenticação customizadas
// Por enquanto, vamos usar Firebase Auth diretamente no controller

import passport from 'passport';

// Configuração do Passport será adicionada aqui se necessário
// Para autenticação com Firebase, não é estritamente necessário

export default passport;
