// Serviço temporário - será implementado futuramente
class CertificateService {
  async generateCertificate(data: any): Promise<string> {
    throw new Error('Funcionalidade de certificado não implementada (fora do escopo atual)');
  }
}

export default new CertificateService();
